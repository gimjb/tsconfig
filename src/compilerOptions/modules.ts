const MODULE_OPTIONS = {
	// allowArbitraryExtensions: false,
	// allowImportingTsExtensions: false,
	// allowUmdGlobalAccess: false,
	// baseUrl: '.',
	// customConditions: [],
	module: 'nodenext',
	// moduleResolution: 'nodenext',    // Default is 'nodenext' if module is 'nodenext'
	// moduleSuffixes: [],
	// noResolve: false,
	noUncheckedSideEffectImports: true,
	// paths: {},
	resolveJsonModule: true,
	// resolvePackageJsonExports: true, // Default is true if moduleResolution is 'nodenext'
	// resolvePackageJsonImports: true, // Default is true if moduleResolution is 'nodenext'
	// rewriteRelativeImportExtensions: false,
	// rootDir: 'src',
	// rootDirs: ['src'],
	// typeRoots: ['node_modules/@types'],
	// types: [],
}

export default MODULE_OPTIONS
